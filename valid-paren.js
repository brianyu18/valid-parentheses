var isValid = function(s) {
   let hash = {
       '(':')',
       '{':'}',
       '[':']'
   }

    let stack = []

    if(stack.length !== 0){
        return false
    } else {
        return true
    }
};
